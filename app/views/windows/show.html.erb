<h1><%= @window.room.name %> Window </h1>
    <p> Window Direction: <%= @window.facing_direction %> </p>
    <p> Light Intensity: <%= @window.light_intensity %> </p>

<h2> Plants in this window </h2>
<% @window.plants_windows.each do |join| %>
    <%= link_to join.plant.name, plant_path(join.plant) %>
    <%# <%= link_to "Delete Plant", join, method: :delete, data: {confirm: "Do you want to delete this plant?"} %> <br> 
<% end %><br>

<%= link_to "Add a Plant", new_plants_window_path %>


